# Port for the local server (serves API + built UI)
PORT=8787

# Optional: enable higher-quality AI generation of cards & quiz questions.
# When unset, StudyForge uses its built-in offline heuristic generator.
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-sonnet-5
